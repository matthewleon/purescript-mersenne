{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "jshint src && jscs src && pulp build",
    "test": "pulp test"
  },
  "devDependencies": {
    "jscs": "^3.0.7",
    "jshint": "^2.9.1",
    "pulp": "^10.0.4",
    "purescript-psa": "^0.4.0",
    "rimraf": "^2.6.1"
  }
}
